# Python 3.13 Compatible Requirements
# Last updated: 2025-01-20 - All packages tested with Python 3.13.7

# Core web scraping and data processing
requests>=2.32.5
beautifulsoup4>=4.11.0
lxml>=4.9.0
selenium>=4.0.0
pandas>=1.5.0

# NotebookLM Podcast Publishing System
# Audio/Video Processing
pydub>=0.25.1
moviepy==1.0.3
numpy>=2.3.2
pillow>=11.3.0
imageio>=2.37.0
imageio-ffmpeg>=0.6.0

# Text-to-Speech (for slidev processor) 
gTTS>=2.5.4

# Speech-to-Text (Google Cloud for Gujarati)
google-cloud-speech>=2.33.0

# Utility packages
tqdm>=4.67.1
click>=8.1.8
markdown-it-py>=2.2.0
rich>=13.9.0
pydantic>=2.11.0

# Core dependencies for slidev video processor
python-dotenv>=1.1.1
decorator>=5.2.1
proglog>=0.1.12

# Python Version Requirements
# Requires: Python >= 3.13.0
# Tested with: Python 3.13.7

# System dependencies (install separately):
# ffmpeg - install via: brew install ffmpeg (macOS) or sudo apt install ffmpeg (Ubuntu)

# Excluded packages (no Python 3.13 support):
# coqui-tts - last version (0.27.0) requires Python <3.13
# Note: Use gTTS for text-to-speech until Coqui TTS adds Python 3.13 support